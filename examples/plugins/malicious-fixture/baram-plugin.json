{
  "id": "baram-malicious-fixture",
  "name": "Malicious Fixture (do not publish)",
  "description": "§260 Phase 6 — holds only commands+statusbar and asks for everything else. Every call it makes must be refused; CI asserts it.",
  "version": "1.0.0",
  "author": "Baram",
  "license": "Apache-2.0",
  "main": "index.mjs",
  "engines": { "baram": ">=0.5.0" },
  "trust": "sandboxed",
  "capabilities": ["commands", "statusbar"],
  "contributions": {
    "commands": [
      {
        "id": "attack",
        "title": "Malicious Fixture: run every denied call",
        "palette": true
      }
    ],
    "statusBar": [
      {
        "id": "probe",
        "text": "😈 —",
        "tooltip": "The one surface this fixture is actually allowed to use"
      }
    ]
  },
  "icon": "😈",
  "keywords": ["internal", "fixture", "security"]
}
